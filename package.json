{
  "name": "keinen-corp-unified",
  "version": "1.0.0",
  "description": "Unified entry point for Keinen Corp",
  "scripts": {
    "dev": "npx browser-sync start --server --files \"*.html, css/*.css, js/*.js\" --port 5501",
    "start": "npx browser-sync start --server --files \"*.html, css/*.css, js/*.js\" --port 5501"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
